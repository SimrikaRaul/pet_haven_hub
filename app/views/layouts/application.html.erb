<nav>
  <%= link_to "Home", root_path %> |
  <% if user_signed_in? %>
    <%= link_to "Sign Out", destroy_user_session_path, method: :delete, data: { turbo : false, confirm: "Are you sure you want to log out?" } %>

  <% else %>
    <%= link_to "Login", new_user_session_path %> |
    <%= link_to "Sign Up", new_user_registration_path %>
  <% end %>
</nav>

<%= yield %>
